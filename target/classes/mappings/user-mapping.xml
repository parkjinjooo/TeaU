<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="MemberDAO">
<insert id="insertMember">
<![CDATA[
	INSERT INTO MEMBER(MEMBER_ID, MEMBER_PASS, MEMBER_NAME, MEMBER_EMAIL, MEMBER_PHONE, MEMBER_ADDRESS)
	VALUES(#{memberId}, #{memberPass}, #{memberName}, #{memberEmail}, #{memberPhone}, #{memberAddress})
]]>
</insert>

<update id="updateMember">
<![CDATA[
	UPDATE MEMBER
	SET MEMBER_PASS = #{memberPass}, MEMBER_EMAIL = #{memberEmail}, MEMBER_NAME = #{memberName}, MEMBER_PHONE = #{memberPhone}, MEMBER_ADDRESS = #{memberAddress}
	WHERE MEMBER_ID = #{memberId}
]]>
</update>

<delete id="deleteMember">
<![CDATA[
	DELETE MEMBER
	WHERE MEMBER_ID = #{memberId}
]]>
</delete>

<select id="getMember" resultType="member">
<![CDATA[
	SELECT * FROM MEMBER
	WHERE MEMBER_ID = #{memberId}
]]>
</select>

</mapper>